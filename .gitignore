# Claude Code agent worktrees and local scratch (transient, never committed)
.claude/worktrees/

# Rust / Cargo (for the upcoming implementation)
/target/
**/*.rs.bk
Cargo.lock.orig

# Failing-seed cluster state kept for post-mortem (#73): by `queso-soak`
# at the repo root, and by `crates/soak`'s tests under that crate's
# directory (cargo runs a test with its package as the working directory) --
# hence unanchored (#92).
soak-failures/

# TLC's on-disk state fingerprint scratch, written to the CWD by local
# model-checking runs (spec/README.md "How to run TLC").
/states/
