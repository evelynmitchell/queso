# Claude Code agent worktrees and local scratch (transient, never committed)
.claude/worktrees/

# Rust / Cargo (for the upcoming implementation)
/target/
**/*.rs.bk
Cargo.lock.orig

# Failing-seed cluster state kept by `queso-soak` for post-mortem (#73).
/soak-failures/
