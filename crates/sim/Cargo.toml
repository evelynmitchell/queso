[package]
name = "queso-sim"
version = "0.1.0"
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "Deterministic discrete-event simulation kernel for Queso (Phase 0: no consensus logic)."

[lints]
workspace = true

[dependencies]
rand = "0.8"

[dev-dependencies]
