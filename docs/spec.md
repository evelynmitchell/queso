<!-- mdbook include shim (issue #80): the content lives in spec/README.md,
     which stays canonical next to the .tla/.cfg files it documents. This
     file exists only so the published book can carry that chapter without
     a second, drifting copy. If you are reading this on GitHub, read
     spec/README.md itself instead. -->

{{#include ../spec/README.md}}
